zig-out/
.zig-cache/
node_modules/
*.log
*.lock
!back/bun.lock
src/shaders/*.glsl.zig
zig-pkg/
/tmp/

# Private long-term memory (nested git inside db/; never push with ada.git)
/db/
# Brain project config sibling of db/ (may hold local embed prefs)
/brain.yaml
# Secrets (mintty/auth later)
.env
# Structured agent logs (local only)
/logs/
# Angela session jsonl (immortal context; local only)
/.angela/sessions/
/.angela/ada-session
# Instance lock / sockets
.back.lock
