# Ada tool allowlist (deny-by-default). Re-read on every tool call.
# Format (gl1 / Angela): one rule per line — `tool` or `tool:params`
# Patterns are full-match regex, not globs: `.` any char, `*` repeats.
# `zen_browser_.*` allows every zen_browser_* tool. Lines starting with # are comments.
#
# House tools are unprefixed (home MCP prefix:false).
# Work tools are unprefixed (work MCP prefix:false) and omitted so Tom
# gates them (power / password / Duo / KVM).
# `shutdown` (home) is omitted so Tom gates powering off this PC.
# Brain/todo use Angela server__tool names.
# Medium/high-risk tools are omitted so they go through Tom (ask mode).

# -- sensors / listen -------------------------------------------------------
current_time
listen

# -- home (low) -------------------------------------------------------------
desk_light
pc_light_color
alarm__create
alarm__list
alarm__update
alarm__delete
alarm__show
alarm__snooze
timer__create
timer__dismiss
timer__show
media_control
tail_eavesdrop_transcript

# -- angela builtins --------------------------------------------------------
goto
loop_start_marker

# -- brain reads ------------------------------------------------------------
brain__search
brain__think
brain__ontology
brain__graph
brain__graphql
brain__get_entity
brain__schema_methods
brain__schema_orphans

# -- todo reads -------------------------------------------------------------
todo__lists
todo__next
todo__tree
todo__view

# -- sandbox collective (low). spawn / message / cancel go through Tom ------
agents_discover
agents_status
agents_wait

# -- host browser (Ada may drive the attached Zen freely; no Tom) ---
zen_browser_.*

# -- computer-use (Ada may move/click/type on the desktop freely; no Tom) ---
window_.*
cursor_position

# -- filesystem reads (file-io, low). write_file/edit go through Tom ---------
list_dir
read_file
grep
find
stat

# -- context analysis (compact, low). compact_session_history stays Tom-gated -
context_analysis
